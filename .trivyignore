# .trivyignore
# CVEs suppressed here must have a written justification.
# Review this file on every dependency upgrade.
# Format: CVE-YYYY-NNNNN  # reason; expires: YYYY-MM-DD

# No suppressions at v0.1.0.
# Add entries here only when a CVE has no fix available and
# the vulnerability does not apply to our usage pattern.
# Each suppression requires a PR comment explaining why.
